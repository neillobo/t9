/* global t9, describe, it, expect, should */

describe('t9()', function () {
  'use strict';

  it('exists', function () {
    expect(t9).to.be.a('function');

  });

  it('does something', function () {
    expect(true).to.equal(false);
  });

  it('does something else', function () {
    expect(true).to.equal(false);
  });

  // Add more assertions here
});
